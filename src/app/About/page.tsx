import { Metadata } from "next";

export const metadata: Metadata = {
    title : "About Page",
    description : "Dibuat oleh Fiky"
}

export default function AboutPage () {
    return (
        <div>
            <h1>About Page</h1>
        </div>
    );
}