import { Metadata } from "next";

export const metadata: Metadata = {
    title : "About Page",
}
export default function  ProductPage () {
    return (
        <div>
            <h2>Product Page</h2>
        </div>
    )
}